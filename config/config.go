package config

// son las configuraciones para cambiar, podemos poner los atributos como colores y asi 
// la velocidad nunca debera ser cero, puesto que no se cargaran los sprites de movimiento mas que idle
// dejarla de 3.5 a 4.5 para que no sea muy rapido ni muy lento

const (
	RobotSpeed = 3.5
	RobotSize  = 48
	CanSize    = 24
	GridMargin = 60

	ScreenWidth  = 978
	ScreenHeight = 640
)

